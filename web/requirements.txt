# https://www.djangoproject.com/download/
Django==3.2.24

# Psycopg is a PostgreSQL for the Python programming language
# It is a wrapper for the libpq, the official PostgreSQL client library.
# https://www.psycopg.org/
psycopg2==2.9.9
